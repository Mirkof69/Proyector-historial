# FastAPI y servidor
fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pydantic>=2.6

# Deep Learning — PyTorch 2.2 + MONAI (reemplaza TensorFlow)
torch>=2.2.0
torchvision>=0.17.0
monai>=1.3.0
timm>=0.9.16

# Explicabilidad CNN — paquete PyPI oficial (provee el import pytorch_grad_cam).
# Antes apuntaba a un commit git inexistente (hash corrupto ...0b0b0b0b) que
# hacía fallar pip install.
grad-cam>=1.5.0

# Cliente HTTP para Docker HEALTHCHECK y callbacks
requests>=2.31

# Procesamiento de imágenes médicas
opencv-python>=4.9
Pillow>=10.2
pydicom>=2.4

# Procesamiento numérico
numpy>=1.26
scipy>=1.12

# Machine Learning y análisis
scikit-learn>=1.4
scikit-image>=0.22
pandas>=2.2

# Visualización
matplotlib>=3.8

# Versionado de modelos
mlflow>=2.10

# Mensajería RabbitMQ
pika>=1.3.2

# HTTP cliente para callbacks Django
httpx>=0.27.0

# Utilidades
python-dotenv>=1.0
aiofiles>=23.2
