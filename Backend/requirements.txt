# Multi-tenancy
django-tenants>=3.5

# Django Core
Django>=5.1
django-environ>=0.11

# REST Framework
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.3

# API Documentation
drf-spectacular>=0.27

# Database (psycopg v3 - compatible con Python 3.13+)
psycopg[binary]>=3.3

# Image Processing
Pillow>=10.2

# OCR de resultados de laboratorio (requiere binario Tesseract instalado en el SO)
pytesseract>=0.3.10

# PDF & Excel Generation
reportlab>=4.2
openpyxl>=3.1
# Lectura de PDF y verificación de firmas digitales embebidas
pypdf>=4.2

# Caching
django-redis>=5.4
redis>=5.0

# WebSocket & Real-time (Django Channels)
channels>=4.0
channels-redis>=4.0
daphne>=4.0

# Utilities
gunicorn>=21.1
requests>=2.31

# Monitoring & Observability
sentry-sdk[django]>=2.0
python-json-logger>=2.0

# Image & Computer Vision (CNN Grad-CAM, analisis ecografias)
opencv-python-headless>=4.9
numpy>=1.26
scipy>=1.12

# ML / AI — analisis laboratorio y scores riesgo
scikit-learn>=1.4
shap>=0.45
joblib>=1.3

# MLflow — versionado de modelos CNN
mlflow>=2.11

# DICOM — lectura de archivos medicos DICOM
pydicom>=2.4

# Extra django utilities
django-guardian>=2.4
django-cryptography>=1.1

# Type stubs for IDE support (elimina falsos positivos de Pyrefly/Pyright)
django-stubs>=5.1
djangorestframework-stubs>=3.15
types-requests>=2.31
types-python-dateutil>=2.8
types-pytz>=2024.1
types-django-filter>=24.3
types-redis>=5.0
types-openpyxl>=3.1
types-Pillow>=10.2
types-PyYAML>=6.0
types-reportlab>=4.2
types-qrcode>=7.4
types-channels>=4.0
types-beautifulsoup4>=4.12
types-markdown>=3.5

# Testing
pytest-django>=4.8
pytest-cov>=5.0

# HTTP async client (llamadas al microservicio FastAPI)
httpx>=0.27

# MFA / 2FA — TOTP compatible con Google Authenticator
pyotp>=2.9
qrcode[pil]>=7.4

# NLP — Chatbot y procesamiento de lenguaje natural
langdetect>=1.0.9
unidecode>=1.4.0
nltk>=3.9.4
hvac>=2.1.0

# Seeding y datos de prueba
Faker>=20.0.0

# Jupyter / Investigacion de Operaciones
jupyter>=1.0
nbformat>=5.9
matplotlib>=3.8
