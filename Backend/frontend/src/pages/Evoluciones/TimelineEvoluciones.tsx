import React from 'react';
import { Empty } from 'antd';

const TimelineEvoluciones: React.FC = () => (
  <Empty description="Vista de línea de tiempo en desarrollo" />
);

export default TimelineEvoluciones;
